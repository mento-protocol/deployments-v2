# Mento Deployments V2 Instructions

For broad Mento protocol context beyond this deployments repo, start with the
private `mento-master-context` checkout when available:

```text
../mento-master-context/.agents/mento-context/README.md
```

Use it to find source-of-truth contracts, monitoring semantics, live-state
recipes, docs, and whitepaper sources. For addresses and ABIs, this repo and
the generated `@mento-protocol/contracts` package remain the source of truth.
For current on-chain state, resolve the address here and verify with live RPC at
an explicit block.
