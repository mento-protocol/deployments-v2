## Codebase Patterns
- Registry lookups: use `lookupOrFail("ContractName:version")` for direct contracts, `lookupProxyOrFail("ContractName")` for proxied contracts
- Proxy types: CELO proxies have prefix `Proxy:`, OZTUP proxies have prefix `TransparentUpgradeableProxy:`
- Config loading: `Config.get()` returns `IMentoConfig`, needs `NETWORK` env var (or `MENTO_CONFIG_CONTRACT`)
- Address lookups: Registry reads both `.treb/registry.json` (uses namespace + chainId) and `.treb/addressbook.json` (uses chainId only)
- Owner pattern: migration owner is the `MigrationMultisig` entry in the addressbook
- TrebScript/ProxyHelper cannot be inherited in tests due to SENDER_CONFIGS env var dependency; use Registry directly instead
- Key V3 identifiers: FPMMFactory (proxy), OracleAdapter (proxy), FactoryRegistry (proxy), VirtualPoolFactory:v3.0.0, Router:v3.0.0, ReserveV2 (proxy), ReserveLiquidityStrategy (proxy), CDPLiquidityStrategy (proxy), BreakerBox:v2.6.5, SortedOracles (proxy), ProxyAdmin, MarketHoursBreaker:v3.0.0, Broker (proxy), ReserveSafe (addressbook)
- Proxy implementation slot (EIP-1967): 0x360894a13ba1a3210667c828492db98dca3e2076cc3735a920a3ca505d382bbc
- Proxy admin slot (EIP-1967): 0xb53127684a568b3173ae13b9f8a6016e243e63b6e8ee1178d6a717850b5d6103
- fs_permissions in foundry.toml already allow reading .treb/registry.json and .treb/addressbook.json
- Implementation addresses in registry use "ContractName:v3.0.0" pattern (e.g. `lookupOrFail("OracleAdapter:v3.0.0")`)
- IFPMMFactory.deployedFPMMAddresses() returns all deployed FPMM pool proxy addresses
- To test init disabled: call initialize() on impl address with vm.expectRevert() — OZ Initializable blocks re-init
- IOracleAdapter getters return typed interfaces (ISortedOracles, IBreakerBox, IMarketHoursBreaker) — cast with `address()` for address comparison
- BreakerBox: `isBreaker()` checks registration, `breakerTradingMode()` returns mode, `isBreakerEnabled()` checks per-feed enablement
- Config: `getFxRateFeedIds()` for FX feeds, `getRateFeedIds()` for all feeds, `getDefaultFPMMParams()` for default FPMM params
- FPMM implementation address: `lookupOrFail("FPMM:v3.0.0")` — use with `IFPMMFactory.isRegisteredImplementation()`
- IRouter import path: `mento-core/swap/router/interfaces/IRouter.sol` — has `factoryRegistry()` and `defaultFactory()`
- IReserveV2 view functions: `isOtherReserveAddress()`, `isReserveManagerSpender()`, `isLiquidityStrategySpender()`, `isStableAsset()`, `isCollateralAsset()`, plus getters returning arrays (e.g. `getStableAssets()`)
- IReserveV2 mutative functions (owner-only): `registerStableAsset()`, `registerCollateralAsset()`, `registerOtherReserveAddress()`, `registerLiquidityStrategySpender()`, `registerReserveManagerSpender()` + corresponding unregister functions
- Per-pool params from config: `config.getFPMMParams(token0, token1)` — no `getFPMMConfigs()` exists
- FPMM totalSupply: use `IERC20(pool).totalSupply()` — not on IFPMM interface
- IERC20 import: `openzeppelin-contracts/contracts/token/ERC20/IERC20.sol`
- `IFPMMFactory.isPool()` and `getPool()` inherited from `IRPoolFactory`
- BiPoolManager: `lookupProxyOrFail("BiPoolManager")` — `getExchangeIds()` + `getPoolExchange()` to enumerate V2 exchanges
- Virtual pool config: `config.getExchangeConfig(asset0, asset1, pricingModule)` returns `(ExchangeConfig, bool found)` — check `createVirtual` flag
- `IVirtualPoolFactory` extends `IRPoolFactory`: use `getPool(t0, t1)`, `isPool()`, `getAllPools()`
- CDP pools: `ICDPLiquidityStrategy(cdpLiquidityStrategy).getPools()` returns all CDP-backed FPMM pools
- CDP config: `ICDPLiquidityStrategy.getCDPConfig(pool)` returns `CDPConfig { stabilityPool, collateralRegistry, stabilityPoolPercentage, maxIterations }`
- Pool config: `LiquidityStrategy.poolConfigs` is public mapping — auto-generated getter returns `(isToken0Debt, lastRebalance, rebalanceCooldown, protocolFeeRecipient, incentives...)` — define custom `IPoolConfigReader` interface to call it
- Liquity contract chain: StabilityPool → `troveManager()` → TroveManager → `borrowerOperations()` → BorrowerOperations → `activePool()` → ActivePool
- IStableTokenV3 import: `mento-core/interfaces/IStableTokenV3.sol` — has `isMinter()`, `isBurner()`, `isOperator()`
- Bold/Liquity imports: `bold/src/Interfaces/IStabilityPool.sol`, `bold/src/Interfaces/ITroveManager.sol`, etc. (remapping: `bold/` → `lib/bold/contracts/`)
- CDPMigrationConfigLib does NOT exist in codebase — derive CDP info from on-chain CDPLiquidityStrategy state instead
- IFPMM inherits IRPool: `token0()`, `token1()`, `tokens()` available on IFPMM cast
- FPMM swap pattern: deal tokens → transfer to pool → `fpmm.swap(amount0Out, amount1Out, to, "")` — pool checks balance deltas for input
- `IRPool.getAmountOut(amountIn, tokenIn)` for swap preview; `getReserves()` for reserve state
- Router swap: `IRouter.swapExactTokensForTokens(amountIn, amountOutMin, Route[], to, deadline)` — requires ERC20 approve to Router first
- Router Route struct: `{from, to, factory}` — factory is the pool factory (e.g. FPMMFactory)
- Router preview: `IRouter.getAmountsOut(amountIn, Route[])` returns `uint256[]` — index 0 is amountIn, index 1 is amountOut for single-hop
- Router pool resolution: `IRouter.poolFor(tokenA, tokenB, factory)` returns the pool address
- Rebalancing: `ILiquidityStrategy(strategy).rebalance(pool)` — callable by anyone, checks cooldown + threshold
- Rebalancing state: `IFPMM.getRebalancingState()` returns 7 values including `priceDifference` and `rebalanceThreshold`
- Rebalancing errors: `LS_COOLDOWN_ACTIVE` (too soon), `LS_POOL_NOT_REBALANCEABLE` (within threshold)
- `ILiquidityStrategy.getPools()` returns all registered pools for a strategy (both Reserve and CDP)
- StableTokenV3 operator role: `sendToPool(sender, pool, amount)` and `returnFromPool(pool, receiver, amount)` do direct `_transfer` (no allowance needed) — used by StabilityPool
- StableTokenV3 minting: `mint(to, value)` is `onlyMinter`; burning: `burn(value)` / `burn(account, value)` is `onlyBurner`

---

## 2026-03-01 - US-001
- What was implemented: Created `test/integration/V3IntegrationBase.t.sol` — an abstract base contract for V3 integration tests
- Files changed: test/integration/V3IntegrationBase.t.sol (new)
- **Learnings for future iterations:**
  - ProxyHelper inherits TrebScript which requires SENDER_CONFIGS env var; cannot be directly inherited in tests. Instead, use Registry directly and reimplement lookup helpers.
  - Registry (from treb-sol) can be instantiated with `new Registry(namespace, registryFile, addressbookFile)` — it reads JSON at construction and uses `block.chainid` for lookups
  - Config.get() uses `vm.deployCode` to deploy a config contract artifact; needs NETWORK env var
  - Fork must be created BEFORE instantiating Registry so block.chainid is correct
  - The build has pre-existing import issues with bold/openzeppelin but they don't affect compilation (warnings only)
  - `deal()` from forge-std can be used as `_mintToken` helper
---

## 2026-03-01 - US-002
- What was implemented: Created `test/integration/StateVerification.t.sol` with tests for proxy→implementation mappings, init protection, and ProxyAdmin on FPMM pool proxies
- Files changed: test/integration/StateVerification.t.sol (new)
- **Learnings for future iterations:**
  - Implementation addresses are registered with version labels: `lookupOrFail("ContractName:v3.0.0")` works for all V3 contracts (OracleAdapter, FPMMFactory, FactoryRegistry, ReserveV2, ReserveLiquidityStrategy, CDPLiquidityStrategy)
  - `IFPMMFactory(fpmmFactory).deployedFPMMAddresses()` returns all deployed FPMM pool proxy addresses for iteration
  - `getProxyAdmin(pool)` reads EIP-1967 admin slot — all FPMM pool proxies should have ProxyAdmin set by FPMMFactory
  - To verify init is disabled on impl: simply call `initialize()` with dummy args and `vm.expectRevert()` — OZ Initializable pattern prevents re-init
  - The V3IntegrationBase helpers `getProxyImplementation()` and `getProxyAdmin()` work for both Celo and OZ transparent proxies
---

## 2026-03-01 - US-003
- What was implemented: Added ownership and access control tests to `test/integration/StateVerification.t.sol`
- Files changed: test/integration/StateVerification.t.sol (modified)
- **Learnings for future iterations:**
  - `IOwnable` from `mento-core/interfaces/IOwnable.sol` provides `owner()`, `transferOwnership()`, and `renounceOwnership()`
  - All V3 contracts (OracleAdapter, FPMMFactory, FactoryRegistry, VirtualPoolFactory, ReserveV2, ReserveLiquidityStrategy, CDPLiquidityStrategy) are owned by `_getOwner()` (MigrationMultisig from addressbook)
  - `makeAddr("name")` from forge-std creates deterministic random addresses for access control tests
  - `vm.prank(address)` + `vm.expectRevert()` is the standard pattern for testing non-owner access denial
  - `FPMMFactory.deployFPMM()` is owner-only — non-owners are rejected
---

## 2026-03-01 - US-004
- What was implemented: Added OracleAdapter and BreakerBox configuration tests to `test/integration/StateVerification.t.sol`
- Files changed: test/integration/StateVerification.t.sol (modified)
- **Learnings for future iterations:**
  - `IOracleAdapter` returns typed interfaces: `sortedOracles()` returns `ISortedOracles`, `breakerBox()` returns `IBreakerBox`, `marketHoursBreaker()` returns `IMarketHoursBreaker` — cast to address for comparison
  - `IBreakerBox.breakerTradingMode(address)` returns uint8 trading mode for a breaker; MarketHoursBreaker should be 3 (trading halted)
  - `IBreakerBox.isBreakerEnabled(address breaker, address rateFeedID)` checks per-feed breaker enablement
  - `config.getFxRateFeedIds()` returns all FX rate feed IDs — use to iterate and verify breaker enablement and rate validity
  - `IOracleAdapter.getFXRateIfValid(address)` returns (numerator, denominator) — both should be non-zero for valid feeds
---

## 2026-03-01 - US-005
- What was implemented: Added FPMMFactory, FactoryRegistry, and Router state verification tests to `test/integration/StateVerification.t.sol`
- Files changed: test/integration/StateVerification.t.sol (modified)
- **Learnings for future iterations:**
  - `IFPMMFactory.defaultParams()` returns `IFPMM.FPMMParams memory` — compare each field individually against `config.getDefaultFPMMParams()`
  - `IFPMMFactory.isRegisteredImplementation(address)` checks if an FPMM impl is registered; the impl address is `lookupOrFail("FPMM:v3.0.0")`
  - `IFactoryRegistry.fallbackPoolFactory()` and `isPoolFactoryApproved()` verify factory registration
  - `IRouter` lives at `mento-core/swap/router/interfaces/IRouter.sol` — has `factoryRegistry()` and `defaultFactory()` view functions
  - Router.defaultFactory() should point to the FPMMFactory proxy
---

## 2026-03-01 - US-006
- What was implemented: Added ReserveV2 configuration tests to `test/integration/StateVerification.t.sol`
- Files changed: test/integration/StateVerification.t.sol (modified)
- **Learnings for future iterations:**
  - `IReserveV2` is already imported in StateVerification.t.sol from US-002 (init protection tests)
  - ReserveSafe should be registered as both `isOtherReserveAddress` and `isReserveManagerSpender` on ReserveV2
  - ReserveLiquidityStrategy should be registered as `isLiquidityStrategySpender` on ReserveV2
  - `registerStableAsset()` is owner-only — non-owners are rejected
  - All addresses needed (reserveSafe, reserveLiquidityStrategy, reserveV2) are already resolved in V3IntegrationBase
---

## 2026-03-01 - US-007
- What was implemented: Created `test/integration/FPMMPoolState.t.sol` with comprehensive pool state verification tests
- Files changed: test/integration/FPMMPoolState.t.sol (new)
- **Learnings for future iterations:**
  - `IFPMMFactory(fpmmFactory).deployedFPMMAddresses()` returns all deployed pool proxies — iterate this for pool-level tests
  - `IFPMMFactory(fpmmFactory).isPool(address)` and `getPool(token0, token1)` are on `IRPoolFactory` parent interface
  - `config.getFPMMParams(token0, token1)` returns per-pool expected params — there is no `getFPMMConfigs()` method
  - FPMM pool tokens are sorted: `token0 < token1` by address
  - FPMM does not expose `totalSupply()` via IFPMM — use `IERC20(pool).totalSupply()` from openzeppelin-contracts
  - IERC20 import path: `openzeppelin-contracts/contracts/token/ERC20/IERC20.sol` (remapped to openzeppelin-contracts-next)
  - Individual pool param getters: `lpFee()`, `protocolFee()`, `protocolFeeRecipient()`, `feeSetter()`, `rebalanceIncentive()`, `rebalanceThresholdAbove()`, `rebalanceThresholdBelow()`
---

## 2026-03-01 - US-008
- What was implemented: Added VirtualPool deployment tests to `test/integration/StateVerification.t.sol` — verifies virtual pools exist for all exchanges marked `createVirtual` in config, tokens match, and `isPool` returns true
- Files changed: test/integration/StateVerification.t.sol (modified)
- **Learnings for future iterations:**
  - BiPoolManager address: `lookupProxyOrFail("BiPoolManager")` — it's a proxied contract
  - `IBiPoolManager.getExchangeIds()` returns all exchange IDs, `getPoolExchange(exchangeId)` returns the `PoolExchange` struct with `asset0`, `asset1`, `pricingModule`
  - `config.getExchangeConfig(asset0, asset1, address(pricingModule))` returns `(ExchangeConfig memory, bool found)` — the `createVirtual` flag indicates if a virtual pool should exist
  - `IRPoolFactory(virtualPoolFactory).getPool(token0, token1)` looks up deployed virtual pool; `IRPool(pool).tokens()` returns sorted token pair
  - Token sorting helper: `(a > b) ? (b, a) : (a, b)` — tokens are always sorted ascending by address
  - `IVirtualPoolFactory` extends `IRPoolFactory` which has `getPool()`, `isPool()`, `getOrPrecomputeProxyAddress()`
---

## 2026-03-01 - US-009
- What was implemented: Created `test/integration/CDPMigrationVerification.t.sol` with tests for CDP migration state: V2 exchange cleanup, Broker role removal, Liquity contract roles (minters, burners, operators), CDPLiquidityStrategy enablement on FPMM, CDPConfig validation, and pool config validation
- Files changed: test/integration/CDPMigrationVerification.t.sol (new)
- **Learnings for future iterations:**
  - CDPMigrationConfigLib referenced in PRD notes does not exist — derive CDP pool info from on-chain `CDPLiquidityStrategy.getPools()` and `getCDPConfig()` instead
  - Liquity contract addresses can be derived by chaining: StabilityPool → `troveManager()` → TroveManager → `borrowerOperations()` → BorrowerOperations (ILiquityBase) → `activePool()` → ActivePool
  - `LiquidityStrategy.poolConfigs` is a public mapping with auto-generated getter — define a custom `IPoolConfigReader` interface to call it, returns all PoolConfig fields as a tuple
  - `IStableTokenV3` (from `mento-core/interfaces/IStableTokenV3.sol`) has `isMinter()`, `isBurner()`, `isOperator()` for role checks
  - Bold/Liquity interfaces use `bold/src/Interfaces/...` import path (remapping: `bold/` → `lib/bold/contracts/`)
  - `IFPMM.liquidityStrategy(address)` returns bool — checks if a strategy is registered on the pool
  - `ICDPLiquidityStrategy.isPoolRegistered(address)` checks if a pool is registered on the strategy (bidirectional check)
  - V2 exchange destruction: iterate BiPoolManager exchanges and verify none match CDP pool token pair
  - TroveManager priceFeed storage: `priceFeed` is `internal` in LiquityBase — read via `vm.load(troveManager, bytes32(uint256(2)))` (slot 2)
  - FXPriceFeed `rateFeedID()` is a public state var — define `IFXPriceFeed` minimal interface to call it
  - Reserve trove identification: iterate `getTroveIdsCount()` + `getTroveFromTroveIdsArray(i)`, check `troveNFT.ownerOf(troveId) == reserveSafe`
  - `ITroveNFT` inherits ERC721 — use `ownerOf(troveId)` for NFT ownership check
  - `ITroveManager.getLatestTroveData(troveId)` returns `LatestTroveData` with `entireDebt`, `entireColl`, `annualInterestRate`, etc.
  - ReserveTroveFactory is NOT in the registry or codebase — test is conditional on registry lookup
---

## 2026-03-01 - US-010
- What was implemented: Added reserve trove and cleanup verification tests to `test/integration/CDPMigrationVerification.t.sol` — FXPriceFeed rateFeedID validation, reserve trove status/interest rate/NFT ownership/debt coverage/collateral checks, and ReserveTroveFactory cleanup verification
- Files changed: test/integration/CDPMigrationVerification.t.sol (modified)
- **Learnings for future iterations:**
  - `priceFeed` is stored as `internal` in LiquityBase, so it cannot be accessed via interface call — use `vm.load(troveManager, bytes32(uint256(2)))` to read storage slot 2
  - FXPriceFeed has `rateFeedID()` as a public state variable — define a minimal `IFXPriceFeed` interface to access it
  - Reserve trove is identified by iterating all troves in TroveManager and finding the one where `troveNFT.ownerOf(troveId) == reserveSafe`
  - `ITroveManager.getTroveIdsCount()` and `getTroveFromTroveIdsArray(i)` allow enumeration of all troves
  - `ITroveNFT` inherits from IERC721Metadata — standard `ownerOf()` works for NFT ownership checks
  - `ITroveManager.getLatestTroveData(troveId)` returns `LatestTroveData` struct with `entireDebt`, `entireColl`, `annualInterestRate`, etc.
  - ReserveTroveFactory does not exist in the registry or codebase — handle conditionally via `registry.lookup()` returning address(0)
  - `LatestTroveData` import path: `bold/src/Types/LatestTroveData.sol`
---

## 2026-03-01 - US-011
- What was implemented: Created `test/integration/FPMMSwap.t.sol` with tests for direct FPMM pool swaps: token0→token1 swap, token1→token0 swap, getAmountOut preview accuracy, and reserve update verification
- Files changed: test/integration/FPMMSwap.t.sol (new)
- **Learnings for future iterations:**
  - FPMM swap pattern: deal tokens to user → transfer to pool → call `fpmm.swap(amount0Out, amount1Out, to, "")` — the pool checks balance deltas to determine input amounts
  - `IRPool.getAmountOut(amountIn, tokenIn)` returns the expected output after fees — use this for preview, then pass the result as `amount0Out`/`amount1Out` to `swap()`
  - For token0→token1 swap: set `amount0Out=0, amount1Out=expectedOut`; for token1→token0: set `amount0Out=expectedOut, amount1Out=0`
  - `IRPool.getReserves()` returns `(reserve0, reserve1, blockTimestampLast)` — verify reserve changes match input/output amounts
  - `makeAddr("name")` creates deterministic test addresses; `deal(token, to, amount)` mints tokens to any address
  - Tests use the first deployed pool from `IFPMMFactory.deployedFPMMAddresses()` — sufficient for validating swap mechanics
---

## 2026-03-01 - US-012
- What was implemented: Created `test/integration/RouterSwap.t.sol` with tests for Router swap flow: token0→token1 swap, token1→token0 swap, and pool resolution via FactoryRegistry verification
- Files changed: test/integration/RouterSwap.t.sol (new)
- **Learnings for future iterations:**
  - Router swap uses `swapExactTokensForTokens(amountIn, amountOutMin, Route[], to, deadline)` — unlike direct FPMM swaps, Router pulls tokens via `transferFrom`, so caller must `approve(router, amount)` first
  - `IRouter.Route` struct: `{from, to, factory}` — use `fpmmFactory` as factory for FPMM pool routing
  - `IRouter.getAmountsOut(amountIn, routes)` returns `uint256[]` with length = routes.length + 1 — index 0 is input, last index is final output
  - `IRouter.poolFor(tokenA, tokenB, factory)` resolves pool address for a given token pair and factory — useful for verifying routing
  - Router.factoryRegistry() and Router.defaultFactory() are view functions for verifying infrastructure wiring
---

## 2026-03-01 - US-013
- What was implemented: Created `test/integration/RebalanceReserve.t.sol` with tests for ReserveLiquidityStrategy rebalancing: large swap to imbalance pool then verify rebalance reduces price difference, cooldown enforcement (LS_COOLDOWN_ACTIVE revert), and threshold check (LS_POOL_NOT_REBALANCEABLE revert when pool is within threshold)
- Files changed: test/integration/RebalanceReserve.t.sol (new)
- **Learnings for future iterations:**
  - `ILiquidityStrategy(reserveLiquidityStrategy).getPools()` returns all pools registered with ReserveLiquidityStrategy — use first pool for rebalancing tests
  - `ILiquidityStrategy.rebalance(pool)` is the main entry point — callable by anyone, but subject to cooldown and threshold checks
  - `IFPMM.getRebalancingState()` returns 7 values: `(oraclePriceNumerator, oraclePriceDenominator, reservePriceNumerator, reservePriceDenominator, reservePriceAboveOraclePrice, rebalanceThreshold, priceDifference)` — use `priceDifference` before/after to verify rebalance effectiveness
  - Cooldown error: `ILiquidityStrategy.LS_COOLDOWN_ACTIVE.selector` — reverts if `block.timestamp < lastRebalance + rebalanceCooldown`
  - Threshold error: `ILiquidityStrategy.LS_POOL_NOT_REBALANCEABLE.selector` — reverts if `priceDifference <= rebalanceThreshold`
  - Pool config readable via `IPoolConfigReader(strategy).poolConfigs(pool)` auto-generated getter — returns `(isToken0Debt, lastRebalance, rebalanceCooldown, protocolFeeRecipient, incentives...)`
  - To imbalance a pool for testing: swap ~10% of reserve0 via the standard deal→transfer→swap pattern
  - Use `vm.warp(block.timestamp + cooldown + 1)` to advance past cooldown for sequential rebalance tests
---

## 2026-03-01 - US-014
- What was implemented: Created `test/integration/RebalanceCDP.t.sol` with tests for CDPLiquidityStrategy rebalancing: large swap to imbalance CDP-backed pool then verify rebalance reduces price difference, cooldown enforcement (LS_COOLDOWN_ACTIVE revert), and threshold check (LS_POOL_NOT_REBALANCEABLE revert when pool is within threshold)
- Files changed: test/integration/RebalanceCDP.t.sol (new)
- **Learnings for future iterations:**
  - CDPLiquidityStrategy rebalancing uses the exact same `ILiquidityStrategy.rebalance(pool)` interface as ReserveLiquidityStrategy — the only difference is which strategy contract address is used
  - CDP pools are retrieved via `ICDPLiquidityStrategy(cdpLiquidityStrategy).getPools()` — same pattern as `ILiquidityStrategy(reserveLiquidityStrategy).getPools()`
  - Pool config (cooldown, incentives, etc.) is read the same way via `IPoolConfigReader(cdpLiquidityStrategy).poolConfigs(pool)` auto-generated getter
  - Same error selectors apply: `ILiquidityStrategy.LS_COOLDOWN_ACTIVE` and `ILiquidityStrategy.LS_POOL_NOT_REBALANCEABLE`
  - The imbalance-then-rebalance test pattern is reusable across both strategy types
---

## 2026-03-01 - US-015
- What was implemented: Created `test/integration/StableTokenRoles.t.sol` with tests for stable token minting and burning roles on CDP-migrated tokens: BorrowerOperations can mint, random address cannot mint, authorized burners (CollateralRegistry, BorrowerOperations, TroveManager, StabilityPool) can burn, Broker cannot mint/burn, StabilityPool as operator can sendToPool/returnFromPool (direct transfer without approval)
- Files changed: test/integration/StableTokenRoles.t.sol (new)
- **Learnings for future iterations:**
  - StabilityPool operator role uses `sendToPool()` and `returnFromPool()` for direct `_transfer` calls — NOT standard `transferFrom` with allowance
  - `IStableTokenV3.mint(address to, uint256 value)` is `onlyMinter`, `burn(uint256)` and `burn(address, uint256)` are `onlyBurner`
  - `sendToPool(sender, poolAddress, amount)` and `returnFromPool(poolAddress, receiver, amount)` are `onlyOperator` — they bypass ERC20 allowance checks
  - `deal(token, address, amount)` works for dealing debt tokens to authorized contracts for burn tests
  - The `_getDebtToken` and `_getLiquityContracts` helper pattern from CDPMigrationVerification.t.sol is reusable across CDP-related test files
---
