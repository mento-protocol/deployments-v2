## Codebase Patterns
- Registry lookups: use `lookupOrFail("ContractName:version")` for direct contracts, `lookupProxyOrFail("ContractName")` for proxied contracts
- Proxy types: CELO proxies have prefix `Proxy:`, OZTUP proxies have prefix `TransparentUpgradeableProxy:`
- Config loading: `Config.get()` returns `IMentoConfig`, needs `NETWORK` env var (or `MENTO_CONFIG_CONTRACT`)
- Address lookups: Registry reads both `.treb/registry.json` (uses namespace + chainId) and `.treb/addressbook.json` (uses chainId only)
- Owner pattern: migration owner is the `MigrationMultisig` entry in the addressbook
- TrebScript/ProxyHelper cannot be inherited in tests due to SENDER_CONFIGS env var dependency; use Registry directly instead
- Key V3 identifiers: FPMMFactory (proxy), OracleAdapter (proxy), FactoryRegistry (proxy), VirtualPoolFactory:v3.0.0, Router:v3.0.0, ReserveV2 (proxy), ReserveLiquidityStrategy (proxy), CDPLiquidityStrategy (proxy), BreakerBox:v2.6.5, SortedOracles (proxy), ProxyAdmin, MarketHoursBreaker:v3.0.0, Broker (proxy), ReserveSafe (addressbook)
- Proxy implementation slot (EIP-1967): 0x360894a13ba1a3210667c828492db98dca3e2076cc3735a920a3ca505d382bbc
- Proxy admin slot (EIP-1967): 0xb53127684a568b3173ae13b9f8a6016e243e63b6e8ee1178d6a717850b5d6103
- fs_permissions in foundry.toml already allow reading .treb/registry.json and .treb/addressbook.json
- Implementation addresses in registry use "ContractName:v3.0.0" pattern (e.g. `lookupOrFail("OracleAdapter:v3.0.0")`)
- IFPMMFactory.deployedFPMMAddresses() returns all deployed FPMM pool proxy addresses
- To test init disabled: call initialize() on impl address with vm.expectRevert() — OZ Initializable blocks re-init
- IOracleAdapter getters return typed interfaces (ISortedOracles, IBreakerBox, IMarketHoursBreaker) — cast with `address()` for address comparison
- BreakerBox: `isBreaker()` checks registration, `breakerTradingMode()` returns mode, `isBreakerEnabled()` checks per-feed enablement
- Config: `getFxRateFeedIds()` for FX feeds, `getRateFeedIds()` for all feeds, `getDefaultFPMMParams()` for default FPMM params
- FPMM implementation address: `lookupOrFail("FPMM:v3.0.0")` — use with `IFPMMFactory.isRegisteredImplementation()`
- IRouter import path: `mento-core/swap/router/interfaces/IRouter.sol` — has `factoryRegistry()` and `defaultFactory()`
- IReserveV2 view functions: `isOtherReserveAddress()`, `isReserveManagerSpender()`, `isLiquidityStrategySpender()`, `isStableAsset()`, `isCollateralAsset()`, plus getters returning arrays (e.g. `getStableAssets()`)
- IReserveV2 mutative functions (owner-only): `registerStableAsset()`, `registerCollateralAsset()`, `registerOtherReserveAddress()`, `registerLiquidityStrategySpender()`, `registerReserveManagerSpender()` + corresponding unregister functions
- Per-pool params from config: `config.getFPMMParams(token0, token1)` — no `getFPMMConfigs()` exists
- FPMM totalSupply: use `IERC20(pool).totalSupply()` — not on IFPMM interface
- IERC20 import: `openzeppelin-contracts/contracts/token/ERC20/IERC20.sol`
- `IFPMMFactory.isPool()` and `getPool()` inherited from `IRPoolFactory`

---

## 2026-03-01 - US-001
- What was implemented: Created `test/integration/V3IntegrationBase.t.sol` — an abstract base contract for V3 integration tests
- Files changed: test/integration/V3IntegrationBase.t.sol (new)
- **Learnings for future iterations:**
  - ProxyHelper inherits TrebScript which requires SENDER_CONFIGS env var; cannot be directly inherited in tests. Instead, use Registry directly and reimplement lookup helpers.
  - Registry (from treb-sol) can be instantiated with `new Registry(namespace, registryFile, addressbookFile)` — it reads JSON at construction and uses `block.chainid` for lookups
  - Config.get() uses `vm.deployCode` to deploy a config contract artifact; needs NETWORK env var
  - Fork must be created BEFORE instantiating Registry so block.chainid is correct
  - The build has pre-existing import issues with bold/openzeppelin but they don't affect compilation (warnings only)
  - `deal()` from forge-std can be used as `_mintToken` helper
---

## 2026-03-01 - US-002
- What was implemented: Created `test/integration/StateVerification.t.sol` with tests for proxy→implementation mappings, init protection, and ProxyAdmin on FPMM pool proxies
- Files changed: test/integration/StateVerification.t.sol (new)
- **Learnings for future iterations:**
  - Implementation addresses are registered with version labels: `lookupOrFail("ContractName:v3.0.0")` works for all V3 contracts (OracleAdapter, FPMMFactory, FactoryRegistry, ReserveV2, ReserveLiquidityStrategy, CDPLiquidityStrategy)
  - `IFPMMFactory(fpmmFactory).deployedFPMMAddresses()` returns all deployed FPMM pool proxy addresses for iteration
  - `getProxyAdmin(pool)` reads EIP-1967 admin slot — all FPMM pool proxies should have ProxyAdmin set by FPMMFactory
  - To verify init is disabled on impl: simply call `initialize()` with dummy args and `vm.expectRevert()` — OZ Initializable pattern prevents re-init
  - The V3IntegrationBase helpers `getProxyImplementation()` and `getProxyAdmin()` work for both Celo and OZ transparent proxies
---

## 2026-03-01 - US-003
- What was implemented: Added ownership and access control tests to `test/integration/StateVerification.t.sol`
- Files changed: test/integration/StateVerification.t.sol (modified)
- **Learnings for future iterations:**
  - `IOwnable` from `mento-core/interfaces/IOwnable.sol` provides `owner()`, `transferOwnership()`, and `renounceOwnership()`
  - All V3 contracts (OracleAdapter, FPMMFactory, FactoryRegistry, VirtualPoolFactory, ReserveV2, ReserveLiquidityStrategy, CDPLiquidityStrategy) are owned by `_getOwner()` (MigrationMultisig from addressbook)
  - `makeAddr("name")` from forge-std creates deterministic random addresses for access control tests
  - `vm.prank(address)` + `vm.expectRevert()` is the standard pattern for testing non-owner access denial
  - `FPMMFactory.deployFPMM()` is owner-only — non-owners are rejected
---

## 2026-03-01 - US-004
- What was implemented: Added OracleAdapter and BreakerBox configuration tests to `test/integration/StateVerification.t.sol`
- Files changed: test/integration/StateVerification.t.sol (modified)
- **Learnings for future iterations:**
  - `IOracleAdapter` returns typed interfaces: `sortedOracles()` returns `ISortedOracles`, `breakerBox()` returns `IBreakerBox`, `marketHoursBreaker()` returns `IMarketHoursBreaker` — cast to address for comparison
  - `IBreakerBox.breakerTradingMode(address)` returns uint8 trading mode for a breaker; MarketHoursBreaker should be 3 (trading halted)
  - `IBreakerBox.isBreakerEnabled(address breaker, address rateFeedID)` checks per-feed breaker enablement
  - `config.getFxRateFeedIds()` returns all FX rate feed IDs — use to iterate and verify breaker enablement and rate validity
  - `IOracleAdapter.getFXRateIfValid(address)` returns (numerator, denominator) — both should be non-zero for valid feeds
---

## 2026-03-01 - US-005
- What was implemented: Added FPMMFactory, FactoryRegistry, and Router state verification tests to `test/integration/StateVerification.t.sol`
- Files changed: test/integration/StateVerification.t.sol (modified)
- **Learnings for future iterations:**
  - `IFPMMFactory.defaultParams()` returns `IFPMM.FPMMParams memory` — compare each field individually against `config.getDefaultFPMMParams()`
  - `IFPMMFactory.isRegisteredImplementation(address)` checks if an FPMM impl is registered; the impl address is `lookupOrFail("FPMM:v3.0.0")`
  - `IFactoryRegistry.fallbackPoolFactory()` and `isPoolFactoryApproved()` verify factory registration
  - `IRouter` lives at `mento-core/swap/router/interfaces/IRouter.sol` — has `factoryRegistry()` and `defaultFactory()` view functions
  - Router.defaultFactory() should point to the FPMMFactory proxy
---

## 2026-03-01 - US-006
- What was implemented: Added ReserveV2 configuration tests to `test/integration/StateVerification.t.sol`
- Files changed: test/integration/StateVerification.t.sol (modified)
- **Learnings for future iterations:**
  - `IReserveV2` is already imported in StateVerification.t.sol from US-002 (init protection tests)
  - ReserveSafe should be registered as both `isOtherReserveAddress` and `isReserveManagerSpender` on ReserveV2
  - ReserveLiquidityStrategy should be registered as `isLiquidityStrategySpender` on ReserveV2
  - `registerStableAsset()` is owner-only — non-owners are rejected
  - All addresses needed (reserveSafe, reserveLiquidityStrategy, reserveV2) are already resolved in V3IntegrationBase
---

## 2026-03-01 - US-007
- What was implemented: Created `test/integration/FPMMPoolState.t.sol` with comprehensive pool state verification tests
- Files changed: test/integration/FPMMPoolState.t.sol (new)
- **Learnings for future iterations:**
  - `IFPMMFactory(fpmmFactory).deployedFPMMAddresses()` returns all deployed pool proxies — iterate this for pool-level tests
  - `IFPMMFactory(fpmmFactory).isPool(address)` and `getPool(token0, token1)` are on `IRPoolFactory` parent interface
  - `config.getFPMMParams(token0, token1)` returns per-pool expected params — there is no `getFPMMConfigs()` method
  - FPMM pool tokens are sorted: `token0 < token1` by address
  - FPMM does not expose `totalSupply()` via IFPMM — use `IERC20(pool).totalSupply()` from openzeppelin-contracts
  - IERC20 import path: `openzeppelin-contracts/contracts/token/ERC20/IERC20.sol` (remapped to openzeppelin-contracts-next)
  - Individual pool param getters: `lpFee()`, `protocolFee()`, `protocolFeeRecipient()`, `feeSetter()`, `rebalanceIncentive()`, `rebalanceThresholdAbove()`, `rebalanceThresholdBelow()`
---
